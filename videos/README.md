# Input videos (Hugging Face Spaces)

Place **`.mp4`**, **`.mkv`**, **`.mov`**, or **`.webm`** files in this directory and push to your Space. The server uses `VIDEO_ENCODE_VIDEOS_DIR` (default: this folder in the container image).

Non-recursive: only files directly in `videos/` are listed.
